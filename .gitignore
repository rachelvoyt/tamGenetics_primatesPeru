# ignore everything (dirs, files, sub-dirs, sub-files)
*
# but do not ignore .gitignore file
!.gitignore
# and do not ignore the files below (override)
!README.md
!*.Rmd
!*.R
!*.pl
# apply override to all dirs recursively
!**/
# then go back and ignore any archived
*archived*
01_run2_fecalHairBlood/
02_laLab_run1/
05_tamRun5/05_run5GTseq/*.pl
bearTest/
gtscore_vs_gtseq/tamRun3/*.pl
paper2_snpPanel/*.pl
